// Operador de String (String operator)

// camparsion (comparação)
console.log('a' == 'a')

// concatenation (concatenação)
// Retorno a união de duas Strings
let alpha = 'alpha'
alpha += 'bet'
console.log(alpha)